import { DashboardClient } from './dashboard-client';

export const metadata = {
  title: 'Dashboard · Gestión Patrimonial',
};

export default function DashboardPage() {
  return <DashboardClient />;
}
