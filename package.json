{
  "name": "gestionpatrimonial",
  "version": "0.0.0",
  "private": true,
  "description": "Personal financial planner — net worth, multi-bank ingestion, debt modelling, forecasting and optimization insights",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "lefthook": "^1.10.10",
    "typescript": "^5.7.3"
  }
}
